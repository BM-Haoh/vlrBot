# Use this file to compose services locally for development or testing purposes.
# Production deployment is handled via Docker Hub images and the updater script.
version: '3.8'
services:
  bot:
    build: .
    container_name: vlrbot-app
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
    env_file:
      - .env